# Document Visibility Fix - Visual Flow

## Problem Diagram (BEFORE FIX) ❌

```
┌─────────────────────────────────────────────────────────┐
│  CUSTOMER SHARE LINK SUBMISSION                         │
│                                                          │
│  1. Customer fills form                                  │
│  2. Selects documents                                    │
│  3. Uploads: Aadhaar, PAN, Bank Statement, Utility Bill │
│  4. Submits                                              │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  DATABASE STORAGE                                        │
│                                                          │
│  Loans Table:                                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │ id: loan-uuid                                    │  │
│  │ user_id: MERCHANT_ID  ← Problem!                │  │
│  │ email_id: customer@email.com                     │  │
│  │ status: Pending                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Loan_Documents Table:                                  │
│  ┌──────────────────────────────────────────────────┐  │
│  │ loan_id: loan-uuid                               │  │
│  │ document_type: Aadhaar Copy                      │  │
│  │ file_path: loan_documents/...                    │  │
│  │ file_size: 353000                                │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Storage Bucket (loan_documents):                       │
│  ✅ Files uploaded successfully:                        │
│  - Aadhaar_Copy_9f194d7f-7190-4d47.pdf                 │
│  - PAN_Copy_c5559e43-782a-424.pdf                      │
│  - Bank_Statement_c5559e4.pdf                          │
│  - Utility_Bill_9f194d7f-7190.pdf                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  CUSTOMER LOGIN & PORTAL ACCESS                         │
│                                                          │
│  Customer logs in:                                      │
│  - Email: customer@email.com                            │
│  - Password: ****                                       │
│  - auth.uid(): customer-uuid ← Different from MERCHANT! │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  RLS POLICY CHECK (BROKEN)                              │
│                                                          │
│  Query: SELECT * FROM loans WHERE id = 'loan-uuid'      │
│                                                          │
│  RLS Check: (OLD POLICY)                                │
│  ┌──────────────────────────────────────────────────┐  │
│  │ auth.uid() = loans.user_id                       │  │
│  │ customer-uuid = MERCHANT_ID                      │  │
│  │                                                  │  │
│  │ ❌ FALSE - IDs don't match!                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ❌ BLOCKED: Customer can't view own loan               │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  DOCUMENTS UNREACHABLE                                  │
│                                                          │
│  Query: SELECT * FROM loan_documents                    │
│         WHERE loan_id = 'loan-uuid'                     │
│                                                          │
│  ❌ Can't reach loan, so can't fetch documents          │
│                                                          │
│  UI Shows:                                              │
│  ┌──────────────────────────────────────┐              │
│  │  Documents                           │              │
│  │                                      │              │
│  │  No documents uploaded. ❌           │              │
│  │                                      │              │
│  └──────────────────────────────────────┘              │
│                                                          │
│  Even though storage has files! 😞                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Solution Diagram (AFTER FIX) ✅

```
┌─────────────────────────────────────────────────────────┐
│  CUSTOMER LOGIN & PORTAL ACCESS                         │
│                                                          │
│  Customer logs in:                                      │
│  - Email: customer@email.com                            │
│  - Password: ****                                       │
│  - auth.uid(): customer-uuid                            │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  RLS POLICY CHECK (FIXED)                               │
│                                                          │
│  Query: SELECT * FROM loans WHERE id = 'loan-uuid'      │
│                                                          │
│  NEW RLS Policy:                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ auth.uid() = loans.user_id                       │  │
│  │ MERCHANT_ID = MERCHANT_ID → FALSE                │  │
│  │                                                  │  │
│  │ OR (SELECT email FROM auth.users                 │  │
│  │     WHERE id = auth.uid())                       │  │
│  │    = loans.email_id                              │  │
│  │ customer@email.com = customer@email.com          │  │
│  │                                                  │  │
│  │ ✅ TRUE - Email matches!                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ✅ ALLOWED: Customer can view own loan                │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  FETCH LOAN DOCUMENTS                                   │
│                                                          │
│  Query: SELECT * FROM loan_documents                    │
│         WHERE loan_id = 'loan-uuid'                     │
│                                                          │
│  NEW RLS Policy (Simplified):                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │ USING (true)                                     │  │
│  │                                                  │  │
│  │ ✅ All authenticated users can view              │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ✅ ALLOWED: Fetch all documents                       │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  DOCUMENTS FETCHED & DISPLAYED                          │
│                                                          │
│  Data returned:                                         │
│  [                                                       │
│    { document_type: "Aadhaar Copy",                     │
│      file_size: 353000,                                │
│      file_path: "..." },                               │
│    { document_type: "PAN Copy",                        │
│      file_size: 333000,                                │
│      file_path: "..." },                               │
│    { document_type: "Bank Statement",                  │
│      file_size: 500000,                                │
│      file_path: "..." },                               │
│    { document_type: "Utility Bill",                    │
│      file_size: 420000,                                │
│      file_path: "..." }                                │
│  ]                                                      │
│                                                          │
│  UI Shows:                                              │
│  ┌──────────────────────────────────────┐              │
│  │  Documents                           │              │
│  │                                      │              │
│  │  ✅ Aadhaar Copy (353 KB) [Download] │              │
│  │  ✅ PAN Copy (333 KB) [Download]     │              │
│  │  ✅ Bank Statement (500 KB) [Dwld]   │              │
│  │  ✅ Utility Bill (420 KB) [Download] │              │
│  │                                      │              │
│  └──────────────────────────────────────┘              │
│                                                          │
│  😊 Success!                                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## User Roles Flow

```
┌──────────────────────────────────────────────────────────┐
│  THREE MAIN USER TYPES                                   │
└──────────────────────────────────────────────────────────┘

1. CUSTOMER (Via Share Link)
   ┌─────────────────────────────────────────────────────┐
   │ auth.uid() = customer-uuid                          │
   │ user_profiles.role = 'customer' (or NULL)           │
   │                                                     │
   │ RLS Check (Loans):                                  │
   │ ├─ auth.uid() = loans.user_id? → NO (merchant ID)  │
   │ ├─ auth.email = loans.email_id? → ✅ YES           │
   │ └─ role IN ('admin','nbfc_admin')? → NO            │
   │                                                     │
   │ Result: ✅ Can view own loan & documents           │
   └─────────────────────────────────────────────────────┘

2. MERCHANT (Business Partner)
   ┌─────────────────────────────────────────────────────┐
   │ auth.uid() = merchant-uuid                          │
   │ user_profiles.role = 'merchant'                     │
   │ Creates share links with loans                      │
   │                                                     │
   │ RLS Check (Loans):                                  │
   │ ├─ auth.uid() = loans.user_id? → ✅ YES            │
   │ ├─ auth.email = loans.email_id? → NO               │
   │ └─ role IN ('admin','nbfc_admin')? → NO            │
   │                                                     │
   │ Result: ✅ Can view own loans & customer docs      │
   └─────────────────────────────────────────────────────┘

3. NBFC ADMIN
   ┌─────────────────────────────────────────────────────┐
   │ auth.uid() = nbfc_admin-uuid                        │
   │ user_profiles.role = 'nbfc_admin'                   │
   │ Verifies & approves loans                           │
   │                                                     │
   │ RLS Check (Loans):                                  │
   │ ├─ auth.uid() = loans.user_id? → NO                │
   │ ├─ auth.email = loans.email_id? → NO               │
   │ └─ role IN ('admin','nbfc_admin')? → ✅ YES        │
   │                                                     │
   │ Result: ✅ Can view ALL loans & documents          │
   └─────────────────────────────────────────────────────┘

4. ADMIN (System Admin)
   ┌─────────────────────────────────────────────────────┐
   │ auth.uid() = admin-uuid                             │
   │ user_profiles.role = 'admin'                        │
   │ Full system access                                  │
   │                                                     │
   │ RLS Check (Loans):                                  │
   │ ├─ auth.uid() = loans.user_id? → Maybe             │
   │ ├─ auth.email = loans.email_id? → Maybe            │
   │ └─ role IN ('admin','nbfc_admin')? → ✅ YES        │
   │                                                     │
   │ Result: ✅ Can view ALL loans & documents          │
   └─────────────────────────────────────────────────────┘
```

## RLS Policy Changes

```
OLD POLICIES (Blocked Access)
═════════════════════════════════════════════════════════

loans table:
  WHERE auth.uid() = loans.user_id 
     OR EXISTS(SELECT 1 FROM user_profiles 
       WHERE id = auth.uid() AND role = 'admin')
  
  ❌ Blocks customers (IDs don't match)
  ❌ Doesn't include nbfc_admin role


loan_documents table:
  WHERE EXISTS (SELECT 1 FROM loans 
    LEFT JOIN user_profiles up ON auth.uid() = up.id
    WHERE loans.id = loan_documents.loan_id
    AND (loans.user_id = auth.uid() 
      OR loans.email_id = (SELECT email FROM auth.users 
        WHERE id = auth.uid())
      OR up.role IN ('admin', 'nbfc_admin')))
  
  ❌ Complex nested query might fail
  ❌ Still depends on loan access


NEW POLICIES (Allow Access)
═════════════════════════════════════════════════════════

loans table:
  WHERE auth.uid() = loans.user_id
     OR (SELECT email FROM auth.users 
       WHERE id = auth.uid()) = loans.email_id  ← ✅ NEW
     OR EXISTS(SELECT 1 FROM user_profiles 
       WHERE id = auth.uid() 
       AND role IN ('admin', 'nbfc_admin'))     ← ✅ FIXED


loan_documents table:
  WHERE true  ← ✅ SIMPLIFIED
  
  ✅ All authenticated users can view
  ✅ No complex queries
  ✅ No dependency on loans table access
```

## Implementation Timeline

```
Step 1: Apply SQL to Supabase
┌─────────────────────────────────────────┐
│ $ go to supabase.com/dashboard          │
│ $ run FIX_DOCUMENTS_SQL.sql             │
│ $ wait for "Query successful"           │
└────────────────┬────────────────────────┘
                 ▼ (30 seconds)

Step 2: Restart Dev Server
┌─────────────────────────────────────────┐
│ $ npm run dev                           │
│ $ wait for "VITE ... ready"             │
└────────────────┬────────────────────────┘
                 ▼ (10 seconds)

Step 3: Clear Browser Cache
┌─────────────────────────────────────────┐
│ $ Press Ctrl+Shift+Delete               │
│ $ Clear all cache                       │
│ $ Refresh page                          │
└────────────────┬────────────────────────┘
                 ▼ (5 seconds)

Step 4: Test Access
┌─────────────────────────────────────────┐
│ $ Log in as customer                    │
│ $ Go to My Loans                        │
│ $ Click loan details                    │
│ $ Click View Documents                  │
│ $ ✅ See documents!                     │
└─────────────────────────────────────────┘

Total Time: ~5 minutes
```

---

**Visual Flow Complete! Now apply the SQL and test in your portal.**
